build/
out/
.vs/
.vscode/
*.user
CMakeUserPresets.json
vcpkg_installed/
.DS_Store

# Локальный ярлык для запуска + его лончер (зашит абсолютный путь к Qt на
# этой машине — не переносимый артефакт проекта, см. scripts/windows/).
*.lnk
scripts/windows/run-lusakey.cmd

# Собранные релизные артефакты (инсталлятор + портативный zip) — большие
# бинарники, генерируются scripts/windows/build-release.ps1, публикуются
# в GitHub Releases, а не хранятся в самом репозитории.
/release/
